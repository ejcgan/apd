program: spd/experiments/tms/tms_decomposition.py
method: grid
metric:
  name: final_closeness
  goal: minimize
parameters:
  topk_l2_coeff:
    values: [3e-3, 1e-2, 3e-2, 3e-1, 1, 3]
command:
- ${env}
- ${interpreter}
- ${program}
- spd/experiments/tms/tms_topk_config.yaml